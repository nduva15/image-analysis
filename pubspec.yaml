name: varroa_counter
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  camera: ^0.10.0
  # Waggle
  waggle_icons: ^0.0.1
  waggle_value_failures: ^0.1.0
  # waggle_auth:
  #   path: packages/waggle_auth
  waggle_auth:
    git:
      url: https://gitlab+deploy-token-1014862:Z_pZ9vB9bq5xnxS4Cjdz@gitlab.com/vatorex/flutter-waggle.git
      ref: develop
      path: packages/waggle_auth
  waggle_widgets:
    git:
      url: https://gitlab+deploy-token-1014862:Z_pZ9vB9bq5xnxS4Cjdz@gitlab.com/vatorex/flutter-waggle.git
      ref: develop
      path: packages/waggle_widgets
  # Firebase
  firebase_core: ^1.17.0
  firebase_auth: ^3.3.18
  firebase_analytics: ^9.1.8
  firebase_crashlytics: ^2.8.0
  firebase_messaging: ^11.4.0
  firebase_remote_config: ^2.0.7
  firebase_storage: ^10.2.16
  cloud_firestore: ^3.1.16
  flutter_facebook_auth: ^4.2.0
  google_sign_in: ^5.2.3
  sign_in_with_apple: ^4.0.0
  fake_cloud_firestore: ^1.2.0
  firebase_auth_mocks: ^0.8.2
  google_sign_in_mocks: ^0.2.1
  firebase_dynamic_links: ^4.2.4
  # Stack
  flutter_bloc: ^8.0.1
  rxdart: ^0.27.3
  injectable: ^1.5.3
  auto_route: ^4.2.1
  build_runner: ^2.2.0
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  auto_route_generator: ^4.2.0
  bloc_test: ^9.0.2 
  freezed_annotation: ^2.1.0


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

flutter_intl:
  enabled: true
  class_name: Localize
  main_locale: en
  arb_dir: lib/infrastructure/data/l10n/
  output_dir: lib/application/l10n/
  use_deferred_loading: false
  localizely:
    project_id: 79d1a8d2-3261-46ca-bb8b-e89a71087e31

localizely:
  project_id: 79d1a8d2-3261-46ca-bb8b-e89a71087e31
  ota_enabled: true
  upload_tag_added: [ "New" ]
